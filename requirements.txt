# Runtime dependencies for the detection web app.
#
# ultralytics pulls in torch, torchvision, opencv and numpy transitively, so
# they are deliberately not repeated here.
#
# Training the satellite model additionally needs the vendored YOLOv5 stack:
#     pip install -r yolov5/requirements.txt
flask==3.0.3
flask-cors==5.0.0
ultralytics==8.3.28
pillow==10.4.0
gunicorn==23.0.0
# Imported directly by train_satellite.py to write the dataset config.
pyyaml>=6.0
# The satellite model is a YOLOv5-repo checkpoint, so serving it runs the
# vendored yolov5/ code, which still imports pkg_resources. That was removed
# from setuptools 81 and is not in the stdlib on Python 3.12+.
setuptools<81
